<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="@color/background"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="4">

        <TextView
            android:id="@+id/month"
            android:layout_marginLeft="10dp"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:textSize="30sp"
            android:text="January"
            android:textColor="@color/colorPrimaryText"
            android:layout_weight="1"
            />

        <TextView
            android:id="@+id/year"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:textSize="30sp"
            android:text="2016"
            android:textColor="@color/colorPrimaryText"
            android:layout_weight="3"
            />

    </LinearLayout>


    <com.github.sundeepk.compactcalendarview.CompactCalendarView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/compactcalendar_view"
        android:layout_width="fill_parent"
        android:paddingRight="10dp"
        android:paddingLeft="10dp"
        android:layout_height="match_parent"
        app:compactCalendarTextSize="15sp"
        app:compactCalendarBackgroundColor="@color/colorText"
        app:compactCalendarTextColor="@color/colorSecondaryText"
        app:compactCalendarCurrentSelectedDayBackgroundColor="@color/colorLightPrimary"
        app:compactCalendarCurrentDayBackgroundColor="@color/colorLightPrimary"
        app:compactCalendarMultiEventIndicatorColor="#fff"
        />

</LinearLayout>